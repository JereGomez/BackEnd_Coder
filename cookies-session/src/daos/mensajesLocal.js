import ContenedorLocal from "../contenedores/contenedorLocal.js";

class ContenedorDaoLocal extends ContenedorLocal{
    constructor(arch){
        super(arch);
    }
}

export default ContenedorLocal