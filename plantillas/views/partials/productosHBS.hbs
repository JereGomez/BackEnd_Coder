<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Productos</title>
</head>
<body>
    {{#if prod}}
        <table>
          <thead  style="text-align: center">
            <th>Nombre</th>
            <th>precio</th>
            <th>url</th>
          </thead>
          <tbody>
            {{#each productos }}
              <tr style="text-align: center">
                    <td >{{this.title}}</td>
                    <td >{{this.price}}</td>
                    <td><img src={{this.thumbnail}} style="width: 50px; , height: 50px"></td>
                </tr>
            {{/each}}
          </tbody>
        </table>
       {{else}}
        <h4>No se encontraron resultados</h4>
    {{/if}}
    <br>
    <form action="/productos" method="post">
            <button action="../" method="get"> Hacia formulario</button>
    </form>

</body>
</html>

